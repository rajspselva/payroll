create table login (Username varchar(255),
 Password varchar(255),);
 select *from emp;

 select * from login;
 
 INSERT INTO login(username,password)
VALUES ('nagu2','nagu');
select *from login;
create table employee(firstname varchar(255),lastna
